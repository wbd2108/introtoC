#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, const char *argv[]) {
int y,x,count,k;
double z,p,w;
float ff;
printf("enter x here:");
scanf("%d", &x);
printf("%d\n",x);
p=x;
y=log(p)/log(8.0);

for(count==y; count==0; count--)
k=(x/8^(count));
printf("%d", k);
printf(" x8^");
printf("%d", count);


return 0;
}
